# Direkte Abhängigkeiten, exakt gepinnt für reproduzierbare Release-Builds
# (Audit M17). Known-good-Stände, alle Python-3.12-tauglich (CI/Release-Python).
# Transitive Deps (u. a. reportlab via xhtml2pdf) sind bewusst NICHT gepinnt —
# kein Lockfile/Hashes; siehe docs/superpowers/specs/2026-07-05-release-deps-pinnen-design.md
google-auth-oauthlib==1.4.0
google-api-python-client==2.196.0
xhtml2pdf==0.2.17
pyinstaller==6.20.0
holidays==0.99
keyring==25.7.0
# Bewusste Ausnahme von der Transitiv-Regel (sonst pinnen wir nur direkte
# Deps): jaraco.functools/context und importlib_metadata sind Transitive von
# keyring und folgen der Python-Unterstützung als Erste: sie fordern schon
# Python >=3.10. Mit 3.12 als CI-/Release-Python bleibt zwar Puffer (bis 3.10
# war es keiner), das Risiko ist aber dasselbe: zieht jaraco seine
# Skeleton-Pakete auf >=3.13, braeche der 3.12-Build still ueber eine Dep, die
# niemand angefasst hat, und der in CLAUDE.md vorgeschriebene 3.12-Gegencheck
# schlaege nicht an, weil er nur die direkte Dep betrachtet. Die Pins bleiben
# deshalb (ihr Wegfall aenderte zudem die Aufloesung des Release-Builds).
jaraco.functools==4.6.0
jaraco.context==6.1.2
importlib_metadata==9.0.1
pystray==0.19.5
Pillow==12.3.0
dbus-fast==5.0.22; sys_platform == "linux"
pyobjc-framework-Cocoa==12.2.1; sys_platform == "darwin"
